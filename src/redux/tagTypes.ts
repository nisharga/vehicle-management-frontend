export enum tagTypes {
    user = "user",
    driver = "driver",
    trip = "trip",
    vehicle = "vehicle",
  }
  
  export const tagTypesList = [
    tagTypes.user,
    tagTypes.driver,
    tagTypes.trip,
    tagTypes.vehicle,
  ];